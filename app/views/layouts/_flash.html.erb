<% flash.each do |type, message| %>
  <%= content_tag(:div, message, class: "flash flex-1 text-center w-full py-3 items-center font-extrabold #{type == "notice" && "text-green-400" || "text-red-400"}") %>
<% end %>
